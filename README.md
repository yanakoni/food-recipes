# Food Recipes App


## Local run

- `docker compose build`
- `docker compose up postgres`

from api folder
- `cp .env.example .env`
- `npm install`
- `npx prisma migrate`
- run `npm start` from api folder

## Prerequisites

- docker + docker-compose
